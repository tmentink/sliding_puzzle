
// ===========================================
// Config
// ===========================================

  var puzzle = (function(puzzle) {
    "use strict";

    puzzle.config = {
      rowSize: 3,
      puzzleSize: 300,
      mobileSize: 300,
      tabletSize: 450,
      desktopSize: 600
    };

    return puzzle;
  })(puzzle || {});

