
// ===========================================
// Search - Variables
// ===========================================

  var search = (function(search) {
    "use strict";

    // Variables
    // =======================================    
    search.queue;
    search.startNode;
    search.finalState;
    search.currentNode;
    search.visitedStates;

    return search;
  })(search || {});

